name: apps-dawn-fridge-deploy
description: for apps deploy on fridge
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: kubernetes-typescript
